"""Evaluation harness.

    metrics.py   outcome classification and metric aggregation
    baseline.py  the benchmark runner

Shared by Phase 4 (base model), Phase 10 (fine-tuned), and Phase 14's ablation
study. Every configuration in the ablation must be scored by identical code, or
the differences between them measure the scorer rather than the system.
"""
