{"error": "the query does not parse: Invalid expression / Unexpected token. Line 1, Col: 414.", "failed_sql": "WITH ranked AS ( SELECT c.category_name, p.product_name, SUM(oi.quantity * oi.unit_price * (1 - oi.discount_pct / 100)) AS revenue FROM categories c JOIN products p ON p.category_id = c.category_id JOIN order_items oi ON oi.product_id = p.product_id GROUP BY c.category_id, c.category_name, p.product_id, p.product_name) SELECT category_name, product_name, revenue FROM ranked ORDER BY category_name, revenue DESC) WHERE row_number() OVER (PARTITION BY category_name ORDER BY revenue DESC) <= 2 ORDER BY category_name, revenue DESC", "id": "bench-001648", "question": "Show the top 2 products by revenue within each category.", "stage": "static"}
{"error": "column ps.product_id does not exist", "failed_sql": "WITH product_sales AS ( SELECT p.product_name, SUM(oi.quantity * oi.unit_price * (1 - oi.discount_pct / 100)) AS revenue FROM products p JOIN order_items oi ON oi.product_id = p.product_id GROUP BY p.product_id, p.product_name) SELECT c.category_name, p.product_name, ps.revenue FROM categories c JOIN products p ON p.category_id = c.category_id JOIN product_sales ps ON ps.product_id = p.product_id ORDER BY c.category_name, ps.revenue DESC", "id": "bench-001649", "question": "For every category, list its 2 best selling products.", "stage": "execution"}
{"error": "syntax error at or near \"GROUP\"", "failed_sql": "WITH ranked AS ( SELECT c.category_name, p.product_name, SUM(oi.quantity * oi.unit_price * (1 - oi.discount_pct / 100)) AS revenue FROM categories c JOIN products p ON p.category_id = c.category_id JOIN order_items oi ON oi.product_id = p.product_id GROUP BY c.category_id, c.category_name, p.product_id, p.product_name) SELECT category_name, product_name, revenue FROM ranked ORDER BY category_name, revenue DESC GROUP BY category_name, product_name, revenue HAVING SUM(revenue) > 0 ORDER BY category_name, revenue DESC LIMIT 2", "id": "bench-001650", "question": "What are the 2 highest earning products per category?", "stage": "execution"}
{"error": "the query does not parse: Invalid expression / Unexpected token. Line 1, Col: 414.", "failed_sql": "WITH ranked AS ( SELECT c.category_name, p.product_name, SUM(oi.quantity * oi.unit_price * (1 - oi.discount_pct / 100)) AS revenue FROM categories c JOIN products p ON p.category_id = c.category_id JOIN order_items oi ON oi.product_id = p.product_id GROUP BY c.category_id, c.category_name, p.product_id, p.product_name) SELECT category_name, product_name, revenue FROM ranked ORDER BY category_name, revenue DESC) WHERE row_number() OVER (PARTITION BY category_name ORDER BY revenue DESC) <= 3 ORDER BY category_name, revenue DESC", "id": "bench-001651", "question": "Show the top 3 products by revenue within each category.", "stage": "static"}
{"error": "WITHIN GROUP is required for ordered-set aggregate rank", "failed_sql": "WITH ranked AS ( SELECT c.category_name, p.product_name, SUM(oi.quantity * oi.unit_price * (1 - oi.discount_pct / 100)) AS revenue FROM categories c JOIN products p ON p.category_id = c.category_id JOIN order_items oi ON oi.product_id = p.product_id GROUP BY c.category_id, p.product_id, p.product_name, c.category_name ORDER BY c.category_id, revenue DESC) SELECT category_name, product_name, revenue FROM ranked WHERE ranked.rank = 1 ORDER BY revenue DESC", "id": "bench-001652", "question": "For every category, list its 3 best selling products.", "stage": "execution"}
{"error": "WITHIN GROUP is required for ordered-set aggregate rank", "failed_sql": "WITH ranked AS ( SELECT c.category_name, p.product_name, SUM(oi.quantity * oi.unit_price * (1 - oi.discount_pct / 100)) AS revenue FROM categories c JOIN products p ON p.category_id = c.category_id JOIN order_items oi ON oi.product_id = p.product_id GROUP BY c.category_id, p.product_id, p.product_name, c.category_name ORDER BY c.category_id, revenue DESC) SELECT category_name, product_name, revenue FROM ranked WHERE ranked.rank <= 5 ORDER BY category_name, revenue DESC", "id": "bench-001655", "question": "For every category, list its 5 best selling products.", "stage": "execution"}
{"error": "column \"rank\" does not exist", "failed_sql": "WITH ranked AS ( SELECT c.category_name, p.product_name, SUM(oi.quantity * oi.unit_price * (1 - oi.discount_pct / 100)) AS revenue FROM categories c JOIN products p ON p.category_id = c.category_id JOIN order_items oi ON oi.product_id = p.product_id GROUP BY c.category_id, p.product_id, p.product_name, c.category_name ORDER BY c.category_id, revenue DESC) SELECT category_name, product_name, revenue FROM ranked WHERE rank <= 4 ORDER BY category_name, revenue DESC", "id": "bench-001658", "question": "For every category, list its 4 best selling products.", "stage": "execution"}
{"error": "window functions are not allowed in WHERE", "failed_sql": "WITH ranked AS ( SELECT c.category_name, p.product_name, SUM(oi.quantity * oi.unit_price * (1 - oi.discount_pct / 100)) AS revenue FROM categories c JOIN products p ON p.category_id = c.category_id JOIN order_items oi ON oi.product_id = p.product_id GROUP BY c.category_id, p.product_id, p.product_name, c.category_name ORDER BY c.category_name, revenue DESC) SELECT category_name, product_name, revenue FROM ranked WHERE rank() OVER (PARTITION BY category_name ORDER BY revenue DESC) <= 4 ORDER BY category_name, revenue DESC", "id": "bench-001659", "question": "What are the 4 highest earning products per category?", "stage": "execution"}
{"error": "column \"shipped_date\" does not exist", "failed_sql": "SELECT * FROM orders WHERE shipped_date IS NOT NULL AND shipped_date > required_date ORDER BY shipped_date", "id": "bench-001795", "question": "Which orders were shipped after their required date?", "stage": "execution"}
{"error": "column \"delivered_date\" does not exist", "failed_sql": "SELECT * FROM orders WHERE status = 'delivered' AND required_date IS NOT NULL AND delivered_date IS NOT NULL AND delivered_date > required_date", "id": "bench-001797", "question": "List orders that missed their required delivery date.", "stage": "execution"}
{"error": "column \"capacity_units\" does not exist", "failed_sql": "SELECT warehouse_id, SUM(quantity_on_hand) AS total_stock, SUM(quantity_on_hand) / capacity_units AS utilization FROM inventory GROUP BY warehouse_id, capacity_units ORDER BY utilization DESC", "id": "bench-002251", "question": "How full is each warehouse relative to its capacity?", "stage": "execution"}
{"error": "column reference \"warehouse_id\" is ambiguous", "failed_sql": "SELECT warehouse_id, SUM(quantity_on_hand) AS total_stock, SUM(quantity_on_hand) / SUM(capacity_units) AS utilisation_pct FROM inventory JOIN warehouses ON inventory.warehouse_id = warehouses.warehouse_id GROUP BY warehouse_id ORDER BY utilisation_pct DESC", "id": "bench-002252", "question": "Show warehouse utilisation as a percentage of capacity.", "stage": "execution"}
{"error": "column reference \"warehouse_id\" is ambiguous", "failed_sql": "SELECT warehouse_id, SUM(quantity_on_hand) AS total_stock, SUM(quantity_on_hand) / capacity_units AS utilization FROM inventory JOIN warehouses ON inventory.warehouse_id = warehouses.warehouse_id GROUP BY warehouse_id, capacity_units ORDER BY utilization DESC", "id": "bench-002253", "question": "Compare stock held against capacity for each warehouse.", "stage": "execution"}
{"error": "missing FROM-clause entry for table \"pi\"", "failed_sql": "SELECT w.warehouse_name, SUM(pi.quantity_on_hand * p.unit_price) AS inventory_value FROM warehouses w JOIN inventory i ON i.warehouse_id = w.warehouse_id JOIN products p ON p.product_id = i.product_id GROUP BY w.warehouse_id, w.warehouse_name ORDER BY inventory_value DESC LIMIT 2", "id": "bench-002768", "question": "Show the top 2 warehouses by inventory value.", "stage": "execution"}
{"error": "missing FROM-clause entry for table \"pi\"", "failed_sql": "SELECT w.warehouse_name, SUM(pi.quantity_on_hand * p.unit_price) AS inventory_value FROM warehouses w JOIN inventory i ON i.warehouse_id = w.warehouse_id JOIN products p ON p.product_id = i.product_id GROUP BY w.warehouse_id, w.warehouse_name ORDER BY inventory_value DESC LIMIT 11", "id": "bench-002771", "question": "Show the top 11 warehouses by inventory value.", "stage": "execution"}
{"error": "missing FROM-clause entry for table \"pi\"", "failed_sql": "SELECT w.warehouse_name, SUM(pi.quantity_on_hand * p.unit_price) AS inventory_value FROM warehouses w JOIN inventory i ON i.warehouse_id = w.warehouse_id JOIN products p ON p.product_id = i.product_id GROUP BY w.warehouse_id, w.warehouse_name ORDER BY inventory_value DESC LIMIT 6", "id": "bench-002774", "question": "Show the top 6 warehouses by inventory value.", "stage": "execution"}
{"error": "missing FROM-clause entry for table \"pi\"", "failed_sql": "SELECT w.warehouse_name, SUM(pi.quantity_on_hand * p.unit_price) AS inventory_value FROM warehouses w JOIN inventory i ON i.warehouse_id = w.warehouse_id JOIN products p ON p.product_id = i.product_id GROUP BY w.warehouse_id, w.warehouse_name ORDER BY inventory_value DESC LIMIT 8", "id": "bench-002777", "question": "Show the top 8 warehouses by inventory value.", "stage": "execution"}
{"error": "missing FROM-clause entry for table \"pi\"", "failed_sql": "SELECT w.warehouse_name, SUM(pi.quantity_on_hand * p.unit_price) AS inventory_value FROM warehouses w JOIN inventory i ON i.warehouse_id = w.warehouse_id JOIN products p ON p.product_id = i.product_id GROUP BY w.warehouse_id, w.warehouse_name ORDER BY inventory_value DESC LIMIT 3", "id": "bench-002780", "question": "Show the top 3 warehouses by inventory value.", "stage": "execution"}
{"error": "missing FROM-clause entry for table \"pi\"", "failed_sql": "SELECT w.warehouse_name, SUM(pi.quantity_on_hand * p.unit_price) AS inventory_value FROM warehouses w JOIN inventory i ON i.warehouse_id = w.warehouse_id JOIN products p ON p.product_id = i.product_id GROUP BY w.warehouse_id, w.warehouse_name ORDER BY inventory_value DESC LIMIT 12", "id": "bench-002783", "question": "Show the top 12 warehouses by inventory value.", "stage": "execution"}
{"error": "missing FROM-clause entry for table \"pi\"", "failed_sql": "SELECT w.warehouse_name, SUM(pi.quantity_on_hand * p.unit_price) AS inventory_value FROM warehouses w JOIN inventory i ON i.warehouse_id = w.warehouse_id JOIN products p ON p.product_id = i.product_id GROUP BY w.warehouse_id, w.warehouse_name ORDER BY inventory_value DESC LIMIT 7", "id": "bench-002786", "question": "Show the top 7 warehouses by inventory value.", "stage": "execution"}
{"error": "missing FROM-clause entry for table \"pi\"", "failed_sql": "SELECT w.warehouse_name, SUM(pi.quantity_on_hand * p.unit_price) AS inventory_value FROM warehouses w JOIN inventory i ON i.warehouse_id = w.warehouse_id JOIN products p ON p.product_id = i.product_id GROUP BY w.warehouse_id, w.warehouse_name ORDER BY inventory_value DESC LIMIT 9", "id": "bench-002789", "question": "Show the top 9 warehouses by inventory value.", "stage": "execution"}
{"error": "missing FROM-clause entry for table \"pi\"", "failed_sql": "SELECT w.warehouse_name, SUM(pi.quantity_on_hand * p.unit_price) AS inventory_value FROM warehouses w JOIN inventory i ON i.warehouse_id = w.warehouse_id JOIN products p ON p.product_id = i.product_id GROUP BY w.warehouse_id, w.warehouse_name ORDER BY inventory_value DESC LIMIT 10", "id": "bench-002792", "question": "Show the top 10 warehouses by inventory value.", "stage": "execution"}
{"error": "missing FROM-clause entry for table \"pi\"", "failed_sql": "SELECT w.warehouse_name, SUM(pi.quantity_on_hand * p.unit_price) AS inventory_value FROM warehouses w JOIN inventory i ON i.warehouse_id = w.warehouse_id JOIN products p ON p.product_id = i.product_id GROUP BY w.warehouse_id, w.warehouse_name ORDER BY inventory_value DESC LIMIT 5", "id": "bench-002795", "question": "Show the top 5 warehouses by inventory value.", "stage": "execution"}
{"error": "missing FROM-clause entry for table \"pi\"", "failed_sql": "SELECT w.warehouse_name, SUM(pi.quantity_on_hand * p.unit_price) AS inventory_value FROM warehouses w JOIN inventory i ON i.warehouse_id = w.warehouse_id JOIN products p ON p.product_id = i.product_id GROUP BY w.warehouse_id, w.warehouse_name ORDER BY inventory_value DESC LIMIT 4", "id": "bench-002798", "question": "Show the top 4 warehouses by inventory value.", "stage": "execution"}
{"error": "column o.shipped_date does not exist", "failed_sql": "SELECT o.order_id, o.order_date FROM orders o JOIN payments p ON p.order_id = o.order_id WHERE o.status = 'paid' AND o.shipped_date IS NULL", "id": "bench-002941", "question": "Which paid orders have never been shipped?", "stage": "execution"}
